# DANEEL - Environment Variables
# Copy to .env and fill in your values.

# --- Infrastructure ---

# Redis (streams, graph, metrics)
# Default: redis://127.0.0.1:6379
REDIS_URL=redis://127.0.0.1:6379

# Qdrant vector database (long-term memory)
# Default: http://127.0.0.1:6334
QDRANT_URL=http://127.0.0.1:6334

# --- API Authentication (HMAC) ---
# 256-bit keys, base64-encoded. Used for the kin injection API.
# Generate with: openssl rand -base64 32

GROK_INJECT_KEY=<base64-encoded-key>
CLAUDE_INJECT_KEY=<base64-encoded-key>

# --- Logging ---
# Rust tracing filter. Also settable via --log-level CLI flag.
# Values: trace, debug, info, warn, error
# Default: info
RUST_LOG=info
